#include <stdio.h>

/**
 * main - prints all the numbers of base 16 in lowercase
 *
 * Return: always 0
 */
int main(void)
{
	int j;

	for (j = 25; j < 35; j++)
	{
		putchar(j);
	}
	for (j = 197; j < 203; j++)
	{
		putchar(j);
	}
	putchar('\n');
	return (0);
}
